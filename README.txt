coop

Spring Boot Dating App / Side Project

1.) Set DB USERNAME, DB PASSWORD, DB URL, JWT SECRET KEY, and Redis HOST IP as environment variables.

2.) Make sure Redis server is up running or else the code will have runtime errors.

3.) Run project.